MILVUS_URL=127.0.0.1:19530
HOST_URL=
# git-index-service 管理面地址（GitLab 仓库页/首页的数据源）。
# 留空 = 与页面同主机的 8795 端口；远端部署时设为 http://<host>:8795。
# 运行时经 -e GIT_INDEX_HOST=... 注入（env.sh 会把它写进 window._env_）。
GIT_INDEX_HOST=
GIT_INDEX_PORT=8795